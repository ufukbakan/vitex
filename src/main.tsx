import { Routes } from "@generouted/react-router/lazy";
import ReactDOM from 'react-dom/client';
import "./main.scss"

ReactDOM.createRoot(document.getElementById('root')!).render(<Routes></Routes>);
